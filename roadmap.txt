
Ideas for upcoming dojos
------------------------

Parsec:

- json parser

Concurrency:

- forkIO

Real World Haskell:

(IO, db, bigger programs)

- IRC bot based on Haskell Wiki's template
- SQLite
- JSON parser
- HXT
- Regex (what's installed by default on haskell platform)
  * when to use (instead of say parsec), when not to use

Monads:

- ?
